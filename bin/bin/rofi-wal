#! /bin/sh
imagepath="$HOME/Wallpapers/"
chosen=$(ls $imagepath | rofi -dmenu -lines 4  -p "Select Wallpaper" -i )
wal -i $HOME/Wallpapers/$chosen  -o wal-set | sleep 0.5s | notify-send "Theme Changed" -i $HOME/Wallpapers/$chosen -t 3000
